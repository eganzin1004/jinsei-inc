# 最初の1週間

`template/` をコピーしてから、実際に回りはじめるまでの手順です。

**全部やろうとしないでください。** この文書のうち、1日目と1週目だけで動きます。残りは必要になってから読んでください。

## 全体の道筋

```
  段階0  置く                    15分     ┐
  段階1  宛先を作る              30分     ├─ ここまでが初日
  ────────────────────────────────────────┘
  段階2  決定ログだけを書く      1〜2週間  ★ここが本体。部署を増やさない
  ────────────────────────────────────────
  段階3  部署を1つ足す          溜まってから（1ヶ月後でも可）
  段階4  読み返す場所を決める    2週間ぶん溜まってから  ★分岐点
  段階5  自動で動かす            1ヶ月後・任意
```

**進行状況は `template/SETUP.md` が持っています。** そちらがチェックリストで、この文書が手順の詳細です。**秘書役は起動のたびに `SETUP.md` を見て、未完のうち次の1段階だけを出します**——だから、この文書を読み終わったら閉じてしまって構いません。戻ってくる導線は仕組みの側にあります。

読み物として一度で通したいときは、このまま下へ読み進めてください。

---

## 1日目 — 置いて、名前をつける

### コピーする

```bash
cp -r jinsei-inc/template ~/my-os
cd ~/my-os
```

置き場所はどこでも構いませんが、**AIエージェントが日常的に開く場所**にしてください。開かない場所に置くと、それだけで使われなくなります。

### `CLAUDE.md` の「オーナー」節を埋める

ここだけは最初に書きます。空のままだと、エージェントが誰に向かって話しているのか分からないままになります。

書くのは、名前・仕事・生活の形・使っている環境の4つです。**長く書かないでください。** 5行で足ります。詳しく書きたくなったら、それは記録のほうに書くべきものです。

### 秘書役に名前をつける

`secretary/CLAUDE.md` の人格の節に、名前を書きます。

**これは飾りではありません。** 名前がないと、書いたものが誰にも向いていない独り言になります。独り言は主語を省くので、あとで読み返せません。誰か宛てだと思うと主語を書くようになり、主語のある文は3ヶ月後に読めます。

名前は何でも構いませんが、**自分が呼びたい名前**にしてください。呼びたくない名前は呼ばなくなります。

### 動かしてみる

```bash
bash scripts/startup-check.sh
```

まだ何も記録がないので、ほとんど空の出力が出ます。それで正常です。

---

## 1週目 — 決定ログだけ書く

**この週にやることは1つだけです。何かを決めたら `secretary/notes/decisions.md` に書く。**

部署を増やさない。ルールを整えない。フォルダを作らない。

### 書き方

```markdown
## 2026-02-23

### 断る理由は、返事を出す前に自分の側に書く

受けるか迷った依頼は、先に「受けない理由」をメモしてから返事する。理由が書けないなら受ける。

**理由**: 去年、同じ条件の依頼を断ったのに、今年は受けてしまった。なぜ去年断ったのかを思い出せなかった。
**却下した選択肢**: 断ったあとに理由を書く。順序が逆だと、断った自分を正当化する文章になる。
```

**「却下した選択肢」を必ず書いてください。** ここが本体です。

結論だけの記録は、3ヶ月後の自分の役に立ちません。役に立つのは「何と迷ったか」のほうで、当時の選択肢が分かって初めて「あのとき見えていなかったもの」が分かります。書くことがない日は書かなくて構いません。

### 決定を撤回したとき

消さないでください。ステータス行だけ足します。

```markdown
→ ステータス: **廃止**（2026-05-13、「A社の受注量を月2本までに制限する」により）
```

撤回した記録が残っていないと、**成功だけが並んだ経歴**ができあがります。それは自分を励ましますが、判断の材料にはなりません。

---

## 2週目以降 — 部署を1つ足す

書くことが溜まってきた領域から、1つずつ足します。**先にフォルダを作らないでください。**

部署のルールファイル（`◯◯/CLAUDE.md`）は `_department-template.md` を使います。書けないなら、その部署はまだ要りません。**方針のないものにルールは書けません。**

`examples/decisions.md` の 2026-02-16 に、8部署を一度に埋めようとして3日で止まった記録があります。同じことをする必要はありません。

---

## 溜まってきたら — 儀式を足す

記録は書くだけだと溜まる一方なので、**読み返す場所**を決めます。ここを設計しないと、書いたものは使われないまま増えます。

| 儀式 | 頻度 | やること |
|---|---|---|
| 起動チェック | セッションごと | `scripts/startup-check.sh` が期限・停滞を出す |
| 週次の圧縮 | 週1 | 日次ログを週次にまとめ、判断軸を拾ってから消す |
| 月次レビュー | 月1 | 判断軸の精錬・決定の棚卸し・生存確認 |

詳しい手順は `strategy/CLAUDE.md` と `secretary/CLAUDE.md` にあります。

**新しい台帳やログを作るときは、入口（誰が・何をきっかけに書くか）と出口（誰が・何をきっかけに読むか）を必ずセットで決めてください。** 出口を設計できないなら作らないほうがいいです。理由は `docs/design-principles.md` の原則1にあります。

---

## 自動で動かす（任意・macOSのみ）

儀式を「気が向いたら」にすると続きません。時間が来たら勝手に走るようにします。

```bash
cp template/launchd/com.example.company-auto-maintain.plist ~/Library/LaunchAgents/
# plist 内のパスを自分の環境に書き換えてから
launchctl load ~/Library/LaunchAgents/com.example.company-auto-maintain.plist
```

**これは最後でいいです。** 1ヶ月くらい手で回してみて、続きそうだと分かってから入れてください。続かないものを自動化しても、失敗が自動で積み上がるだけです。

Linux / WSL では動きません。cron や systemd timer に置き換えられますが、こちらでは検証していません。

---

## 外部サービスと繋ぐ（任意・後から）

カレンダー・タスク・ノート・メールと繋げられます。**この型には同梱していません。** 何と繋ぐかは人によって違うし、繋がない状態でも仕組みは完結するからです。

繋ぐ手段は MCP（Model Context Protocol）サーバーです。Claude Code に登録すると、エージェントがそのサービスを読み書きできるようになります。

実際にやっている例です。

| 繋ぐ先 | 何に使っているか |
|---|---|
| カレンダー・タスク | 起動時に今日の予定と未処理タスクを読んで、ダッシュボードに出す |
| ノートアプリ | スマホから書いたメモを、あとでまとめて取り込む口にする |
| メール | 特定の送信元だけ拾って要約する |

繋ぐと「**スマホから入れて、PCで処理する**」形が作れます。これは実際に効きました——移動中に思いついたことを取りこぼさなくなります。

ただし2つ注意があります。

- **書き込みは読み取りと同じ扱いにしないこと。** 追加（予定・タスクの作成）は自由にやらせてよいですが、**完了化・削除だけは毎回確認を挟む**設計にしてください（`CLAUDE.md` の行動原則にそう書いてあります）。追加は消えるものがないが、消す操作は取り返しの感覚が違います
- **外部サービスを仕組みの前提にしないこと。** サービスが落ちても止まらないように、状態の正はローカルのファイル側に置きます

**繋ぐのは最後でいいです。** ローカルのファイルだけで1ヶ月回してから考えてください。

---

## つまずきやすいところ

**書くことがない日が続く** — 正常です。毎日書くルールは早い段階で捨てたほうがいいです（`examples/mindset.md` に、それを卒業した記録があります）。

**部署が増えすぎる** — 部署が8つあると、8つ使う理由を探してしまいます。使っていない部署は畳んでください。

**ルールを整えるのが楽しくなる** — これが一番よくある失敗です。ルールを書いた量と、記録を書いた量を、月に一度くらい比べてみてください。整備が使用を上回る月が続いているなら、それは仕組みを作ることが目的になっています。

**AIが同意しかしなくなる** — 記録が厚くなるほど、どんな選好でも「過去の判断軸と一致している」と言えるようになります。対処は `docs/design-principles.md` と `docs/limits.md` に書きましたが、**根本的には解決していません。** そこは正直に書いてあります。
